{
  "name": "stattared4h",
  "version": "0.0.0",
  "private": true,
  "description": "Stättared 4H – dev tooling only (project at starting state)",
  "scripts": {
    "lint:md": "markdownlint \"docs/**/*.md\" README.md CLAUDE.md",
    "lint:md:fix": "markdownlint --fix \"docs/**/*.md\" README.md CLAUDE.md",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.48.0"
  }
}
