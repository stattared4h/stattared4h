---
layout: base.njk
permalink: /
title: Djuren på Stättared
description: >-
  Lär känna djuren på Stättareds 4H-gård — vad de heter, vilka som är släkt med
  varandra, och vilka djur som finns i varje hage. Ett komplement till gårdens
  huvudsida på 4h.se/stattared.
---
{#- The home page (02-§5.7–5.8): what the site is, the species on the farm, and the map. #}
{% import "species-tile.njk" as tiles %}
<div class="container">
  <h1>Lär känna djuren på gården</h1>

  <p class="lead">
    Den här sidan handlar bara om djuren på Stättareds 4H-gård. Öppettider, boende,
    bokningar och nyheter finns på <a href="{{ site.mainSite }}">gårdens huvudsida</a>.
  </p>

  {%- if views.home.species.length > 0 %}
  <section class="page-section">
    <h2>Hitta ett djur via öronmärket</h2>
    <p>Skriv av hela numret. Mellanslag och bindestreck spelar ingen roll.</p>
    <form data-animal-id-search>
      <label for="animal-public-id">Öronmärke</label>
      <input id="animal-public-id" data-animal-id-input type="search" inputmode="text" autocomplete="off" placeholder="SE 012345 0001">
      <button class="button" type="submit">Hitta djuret</button>
      <p data-animal-id-result role="status" aria-live="polite" hidden></p>
      <div hidden aria-hidden="true">
        {%- for animal in dataset.animals %}
          {%- if animal.publicId %}
        <span data-public-id="{{ animal.publicId }}" data-name="{{ animal.name }}" data-url="{{ ('/djur/' + animal.id + '/') | url }}"></span>
          {%- endif %}
        {%- endfor %}
      </div>
    </form>
  </section>

  <section class="page-section">
    <h2>Djuren på gården</h2>
    {{- tiles.speciesGrid(views.home.species, true) }}
  </section>

  <section class="page-section">
    <h2>Var går de?</h2>
    <p>Kartan visar gårdens hagar och vilka djurslag som går i varje hage.</p>
    <p><a class="button" href="{{ '/karta/' | url }}">Karta över gården</a></p>
  </section>
  {%- else %}
  <div class="note page-section">
    <h2>Djuren är inte inlagda ännu</h2>
    <p>
      Snart kan du läsa om varje djur här. Under tiden hittar du allt om gården på
      <a href="{{ site.mainSite }}">4h.se/stattared</a>.
    </p>
  </div>
  {%- endif %}
</div>
