# Module: example-module

> **Owner:** (optional — name or GitHub handle)

One short paragraph describing what this module owns. What part of
the system, what kinds of requirements live here, what is
deliberately *not* part of this module. The reader should be able to
decide in five seconds whether the requirement they're looking for
might be in this folder.

## Requirements

| Requirement | Status | Verification |
| ----------- | ------ | ------------ |
| [example-slug](./example-slug.md) | proposed | — |

Add a row here when a new requirement file lands in this folder.
Update the Status when Phase 5 of the lifecycle moves the underlying
requirement file. This matrix is the only "shared" file for work in
this module, and it is small on purpose — keep it that way.

Verification values: `test`, `manual`, or `—` (only while status is
`proposed`). See [`../index.md`](../index.md) for the full
convention.
